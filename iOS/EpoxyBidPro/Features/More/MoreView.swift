import SwiftUI

struct MoreView: View {
    var body: some View {
        NavigationStack {
            Text("Settings, reports, invoicing")
                .navigationTitle("More")
        }
    }
}
