import SwiftUI

struct ScanView: View {
    var body: some View {
        NavigationStack {
            Text("LiDAR scanner placeholder")
                .navigationTitle("New Bid Scan")
        }
    }
}
